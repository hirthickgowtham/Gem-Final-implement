// import bcrypt from "bcrypt";

// const generateHash = async () => {
//     const password = "123"; // change to your real admin password
//     const hashedPassword = await bcrypt.hash(password, 10);
//     console.log("Hashed Password:", hashedPassword);
// };

// generateHash();
